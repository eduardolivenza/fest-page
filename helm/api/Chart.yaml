apiVersion: v2
name: festpage-api
description: Fest Descans — REST API (Fastify) + PostgreSQL
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: postgresql
    version: ">=13.0.0"
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
