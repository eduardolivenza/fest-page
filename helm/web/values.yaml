image:
  repository: ghcr.io/eduardolivenza/festpage/web
  tag: v0.1.2
  pullPolicy: IfNotPresent
replicas: 1
service:
  port: 3000
resources:
  requests:
    memory: 128Mi
    cpu: 100m
  limits:
    memory: 512Mi
    cpu: 500m
# Public URL of the API — baked in at build time via NEXT_PUBLIC_API_URL,
# but also set as a runtime env var for server-side fetches.
apiUrl: https://api.festdescans.com
ingress:
  className: traefik
  host: festdescans.com
  tls:
    enabled: false
    secretName: fest-descans-web-tls
    clusterIssuer: letsencrypt-prod
global:
  imagePullSecrets: []
