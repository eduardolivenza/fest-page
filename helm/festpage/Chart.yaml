apiVersion: v2
name: festpage
description: Fest Descans — public storefront, admin panel, and REST API
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: postgresql
    version: ">=13.0.0"
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
