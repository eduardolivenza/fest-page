apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-admin
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "festpage.labels" . | nindent 4 }}
    app.kubernetes.io/component: admin
spec:
  selector:
    {{- include "festpage.selectorLabels" (dict "root" . "component" "admin") | nindent 4 }}
  ports:
    - port: 3002
      targetPort: 3002
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-admin
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "festpage.labels" . | nindent 4 }}
    app.kubernetes.io/component: admin
spec:
  replicas: {{ .Values.admin.replicas }}
  selector:
    matchLabels:
      {{- include "festpage.selectorLabels" (dict "root" . "component" "admin") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "festpage.selectorLabels" (dict "root" . "component" "admin") | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: admin
          image: {{ include "festpage.image" (dict "registry" .Values.global.imageRegistry "repo" .Values.admin.image.repository "tag" .Values.admin.image.tag) }}
          imagePullPolicy: {{ .Values.admin.image.pullPolicy }}
          ports:
            - containerPort: 3002
          env:
            - name: NODE_ENV
              value: production
            - name: NEXT_PUBLIC_API_URL
              value: {{ printf "https://%s" .Values.ingress.hosts.api }}
          readinessProbe:
            httpGet:
              path: /login
              port: 3002
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            {{- toYaml .Values.admin.resources | nindent 12 }}
